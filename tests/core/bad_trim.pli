 /* extension (ADR-107): TRIM arity and argument
    types are diagnosed. */
 badtrim: procedure options(main);
    declare x fixed bin(31);
    x = length(trim(5));
    put skip list(trim());
 end badtrim;
