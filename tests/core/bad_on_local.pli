 /* rule (91): an ON-unit reaching an automatic variable by subscript
    is not implemented in this stage (whole scalars are captured). */
 badonlocal: procedure options(main);
    declare a(10) fixed bin(31);
    a(1) = 0;
    on error a(1) = 1;
    signal error;
 end badonlocal;
