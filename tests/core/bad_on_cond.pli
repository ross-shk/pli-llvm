  /* rule (94): only ERROR is served; other
     conditions are diagnosed, never
     silently accepted. */
  badoncond: procedure options(main);
     on zerodivide put skip list('x');
  end badoncond;
