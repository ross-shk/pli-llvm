   /* rule (94): ERROR and SIZE are served;
     other conditions are diagnosed,
     never silently accepted. */
  badoncond: procedure options(main);
     on zerodivide put skip list('x');
  end badoncond;
