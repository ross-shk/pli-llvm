 /* Dynamic (runtime-extent) array bound restrictions (rule (13)): a dynamic
    lower bound (n:5) is not implemented in this stage and is rejected at parse
    time; a block-scope '*' adjustable extent is rejected because '*' is a
    parameter-only form. */
 bad_dynamic_array_ext: procedure options(main);
    declare n fixed bin(31);

    declare lo(n:5) fixed bin(31);         /* dynamic lower bound not implemented */
    declare star(*) fixed bin(31);         /* '*' only valid on a parameter */
 end bad_dynamic_array_ext;
