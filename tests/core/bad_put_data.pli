 /* SPDX-License-Identifier: Apache-2.0 */
 /* Copyright 2026 Ross S. - plic: a PL/I compiler targeting LLVM */
   /* rule (110): a PUT DATA item must be a
     variable so its name can be written;
     a constant is diagnosed. */
   badputdata: procedure options(main);
      put data(1 + 2);
   end badputdata;
