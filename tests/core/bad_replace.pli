 /* SPDX-License-Identifier: Apache-2.0 */
 /* Copyright 2026 Ross S. - plic: a PL/I compiler targeting LLVM */
  /* extension (ADR-077): only %REPLACE is
     served; other % directives are
     diagnosed. */
  badreplace: procedure options(main);
     %declare x character;
     put skip list('x');
  end badreplace;
