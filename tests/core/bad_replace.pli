  /* extension (ADR-077): only %REPLACE is
     served; other % directives are
     diagnosed. */
  badreplace: procedure options(main);
     %declare x character;
     put skip list('x');
  end badreplace;
