 /* SPDX-License-Identifier: Apache-2.0 */
 /* Copyright 2026 Ross S. - plic: a PL/I compiler targeting LLVM */
 /* extension (ADR-104): a SELECT with no WHEN
    is diagnosed, never silently accepted. */
 badselect: procedure options(main);
    select;
       otherwise put skip list('x');
    end;
 end badselect;
