/* INITIAL reaches the emitted IR as a store. Regression guard: an accepted
   but codegen-ignored INITIAL silently drops the value (see CONTRIBUTING
   worked example). Rule (26). */
 main: procedure options(main);
   declare x fixed bin(31) initial(42);
   put skip list(x);
 end main;
