# Grammar coverage — TR 25.084 rules (1)–(151)

The ledger that ties the implementation to the specification. Status values:

- **M0**, **M1** — implemented in the completed milestones and covered by tests
- **diag** — recognised and rejected with a citation of its rule number
- **M2**-**M9** — planned for that milestone (IMPLEMENTATION-PLAN.md)
- **D1** — explicitly deferred specialized scalar conformance; diagnosed until implemented

| Rules | Feature | Status | Component / test |
|---|---|---|---|
| (1) | `program ::= procedure•••` | M0 | `parser.cpp:parse` / all tests |
| (2) | procedure, entry-namelist, options | M0 | `parseExternalProcedure` / `hello.pli` |
| (3) | entry-namelist (multiple entry names) | M0 | `a, b: PROCEDURE` shares one body; callable by any name (`multientry.pli`); extra names are internal aliases |
| (4) | parameterlist | M0 | `procs.pli` |
| (5) | procedure options (`OPTIONS`, `RECURSIVE`, `RETURNS`) | M0/M1 | `MAIN` honoured; `RETURNS` → function procedures (`func.pli`); `RECURSIVE` accepted |
| (6),(7) | sentencelist, end-clause, multiple closure | M0 | `parseBody` / `loops.pli` (`END OUTER;`) |
| (8) | sentence kinds | M0 | `parseStatement`; internal procedures reach enclosing automatic storage via a static link (ADR-027, `staticlink.pli`) |
| (9),(10) | `DECLARE`, declarationlist | M0 | `parseDeclare` / `ifelse.pli` |
| (11) | declaration, level numbers, factoring | M2 | scalars + level-numbered structures (nested via `1 S, 2 A ..., 2 X, 3 Y ...`) in read/write positions, including array members `2 A(10) ...` (`struct.pli`, `struct_array.pli`); arrays of structures `1 arr(3), 2 x, 2 y` — a dimensioned level item whose element is a structure — accessed as `arr(i).x` (`struct_array_of.pli`, ADR-051); factored lists `DECLARE (A, B) FIXED BIN(31)` with a factored dimension `(X, Y) (5) ...` (`factor.pli`); `INITIAL` in factored form → diag |
| (12),(13) | dimension attribute, bound pairs | partial M2 | fixed-size constant-bounds arrays, single- and multi-axis, row-major layout (`A(m,n)`/`A(lb:ub,...)`), scalar elements (`array.pli`, `array2d.pli`); single-axis dynamic-bounds AUTOMATIC arrays `A(n)`/`A(lb:n)` with a runtime upper bound, extent fixed at block entry, runtime `SUBSCRIPTRANGE` and runtime `LBOUND`/`HBOUND` (`dynamic_array.pli`, ADR-050); a dynamic lower bound `A(lb:ub)` on a local AUTOMATIC single-axis array — both bounds evaluated at entry, extent, `SUBSCRIPTRANGE`, `LBOUND`/`HBOUND`/`DIM`, and array reductions use the live bounds (`dyn_lower.pli`, `bad_dyn_lower.pli`, ADR-058); a dynamic multi-axis array whose first axis is dynamic and later axes are fixed, e.g. `A(n,4)`/`A(lb:5,3)` — a flat row-major buffer of `n × (later extents)` elements, per-axis `SUBSCRIPTRANGE`, runtime first-axis row stride, and `DIM`/reductions over the full extent (`dyn_multi.pli`, `bad_dyn_multi.pli`, ADR-059); a fixed or dynamic array passed to a procedure whose parameter is a dynamic array `x(k)` — the callee sizes the view from the bound argument at entry and bounds-checks and reduces against the live extent, and writes through the parameter are visible in the caller (`dyn_param.pli`, ADR-054); a single-axis dynamic lower-bound parameter `x(l:u)` — both bounds read at entry, live `LBOUND`/`HBOUND`/`DIM`/reductions and remap-by-position writes (`dyn_param_lower.pli`, ADR-090); `*` adjustable-extent array parameters `x(*)` — a single-axis parameter whose extent comes from the caller's actual array (fixed, dynamic-bound, or multi-axis) at call time, passed as a hidden extent argument; bounds-checked and reduced against the live extent, by-reference writes visible in the caller (`star_param.pli`, ADR-055); a dynamic array as a structure member `1 s, 2 v(n) fixed bin(31)` — a bare runtime-sized element buffer allocated at entry whose pointer is stored in the struct field, bounds-checked and addressed by qualification `s.v(i)` on both sides of an assignment (`struct_dyn.pli`); runtime aggregate lengths on a dynamic member — `LBOUND`/`HBOUND`/`DIM` (incl. a dynamic lower bound) and the array reductions `SUM`/`PROD`/`ANY`/`ALL` operate on the runtime-sized member buffer via the stored pointer and the live member bounds, and a scalar member as a built-in argument is diagnosed (`struct_dyn_len.pli`, `bad_struct_dyn_len.pli`, ADR-062); `INITIAL` on a dynamic array — an itemlist of constants (with iteration factors) fills the runtime-sized element buffer at block entry, pre-sized to hold the whole itemlist since the extent is runtime and cannot be count-checked (`dyn_init.pli`, ADR-061); a whole-structure copy `t = s` with a dynamic member — scalars plus each buffer's contents, pointers preserved, extent mismatch traps (`struct_dyn_copy.pli`, ADR-091); a block-scope `*`, a dynamic axis or lower bound beyond the first, and whole-structure values with a dynamic member outside plain assignment (`LIKE`, by-value arguments, function results) → diag (`bad_star_param.pli`, `bad_dyn_param.pli`, `bad_dynamic_array*.pli`, `bad_dyn_lower.pli`, `bad_dyn_multi.pli`, `bad_struct_dyn.pli`); cross-sections → M2 |
| (14),(15) | attribute, data-attribute set | partial M0 | arithmetic/string/`ALIGNED` subset; `POINTER` (an address value) — `DECLARE P POINTER;` a pointer variable whose value is the null pointer (`null()`), the address of a variable (`addr(x)`), or another pointer; pointers assign between each other and compare with `=`/`^=` against the null pointer and other pointers, while mixing a pointer with an arithmetic value or an ordered pointer comparison is diagnosed (`pointer.pli`, `bad_pointer.pli`, ADR-063); `COMPLEX` (a real+imaginary pair) — `DECLARE z COMPLEX;` declares a complex variable holding an `{double,double}` pair, assigned from `COMPLEX(a,b)`, a real value (imag part 0), or another complex, and read back with `REAL`/`IMAG`/`CONJG`; combining COMPLEX with another data attribute is diagnosed (`complex_var.pli`, `bad_complex_var.pli`, ADR-074); `BASED`/`CONTROLLED`/`AREA`/`OFFSET` still M3 |
| (16),(17) | arithmetic attributes, precision, signed integer | partial M0/M2 | practical binary forms; `FIXED DECIMAL(p,q)` scaled by 10^q (ADR-006): a bare fractional literal (`2.5`) is an exact FIXED DECIMAL constant (rule (135)); scale is honoured in `+ - *`, comparison, and assignment conversion with round-half-away rescaling, and `FIXED DECIMAL <-> FLOAT` conversion scales by 10^q (`decimal.pli`, ADR-056); `FIXED BINARY(p,q)` stays 2-based (unscaled, `scaled.pli`); decimal list-directed and DISPLAY I/O prints the 10^q point and reads scaled values back (`decimal_io.pli`, `driver/get_decimal`, ADR-088); FIXED BINARY `+ - *` and unary minus trap to a hard ERROR on overflow (`driver/overflow`, ADR-089); FIXED DECIMAL `+ - *` and `MULTIPLY` trap on a wrapped intermediate while narrowing conversions (and 64-to-32 binary narrowings) trap past the declared digits or width (`driver/decimal_overflow`, ADR-095); `FLOAT -> FIXED` traps outside the destination range in the float domain, so exact `INT_MIN` edges pass and NaN traps (`driver/float_fixed_overflow`, ADR-096); full decimal/precision conformance and SIZE routing → D1/QR2 |
| (18) | string attributes (`BIT`/`CHARACTER`/`VARYING`) | partial M0 | `BIT(1)` and char/varying served (`strings.pli`); `BIT(n>1)` diagnosed as unimplemented (`bad_bitlen.pli`, rule (18)); schedule by corpus impact |
| (19) | `PICTURE` attribute | D1 | ADR-017 |
| (20) | `AREA` attribute | M3 | |
| (21) | `LABEL` attribute | M4 | label variables |
| (22) | `OFFSET` attribute | M3 | |
| (23) | storage classes | diag → M3 | `AUTOMATIC`/`STATIC` accepted, `CONTROLLED` M3 |
| (24) | `DEFINED`/`POSITION` | partial M2 | `Y ... DEFINED X` overlays the storage of an already-declared same-type variable — Y needs no own storage and reads/writes resolve to X's address (`defined.pli`, ADR-047); an undeclared base, a different-type base, a structure, or a base in an enclosing procedure is diagnosed (`bad_defined.pli`); `POSITION` is diagnosed (rule 24); different-type (memory-view) overlays and `DEFINED X(i)` subscripted bases → M2 |
| (25) | `BASED` | partial M2 | `BASED(P)` — the declared structure has no storage of its own; its members are addressed through the POINTER `P` (`rec.a` reads/writes via `P`), and the locator-qualified form `P -> rec.a` does the same; a locator requires a based variable and a POINTER (`based.pli`, `bad_based.pli`, ADR-064); bare `BASED` without an explicit pointer diagnosed; `CONTROLLED`/`AREA`/`OFFSET` → M3 |
| (26)–(32) | `INITIAL` (incl. `CALL`, iteration, `*`) | partial M0 → M2 | scalar constants (M0); arrays: an `INITIAL` itemlist with iteration factors `(n)`, `*` repeat-last, and nested groups fills a fixed-size numeric/BIT array element-by-element on AUTOMATIC storage (`init_array.pli`, `bad_init_array.pli`, ADR-044); a count that does not match the extent is diagnosed; `INITIAL CALL` `INITIAL(CALL f(args))` calls a value-returning function at block entry and stores the result (rule (27), `init_call.pli`, `bad_init_call.pli`, ADR-052), diagnosed in a factored declaration; `INITIAL` on a structure fills the scalar leaves in declaration order on AUTOMATIC storage, recursing through nested structures and array members, with iteration factors and `*` supported and a count mismatch or static storage diagnosed (`struct_init.pli`, `bad_struct_init.pli`, ADR-057); `INITIAL` on a dynamic array — an itemlist of constants (with iteration factors) fills the runtime-sized element buffer at block entry; the extent is runtime so no compile-time count check, the buffer is pre-sized to hold the whole itemlist, and it re-runs on every activation (`dyn_init.pli`, ADR-061); `INITIAL` on a structure with a trailing dynamic array member — the flat itemlist fills the static leaves first (count-checked) then one buffer element per remaining value with no count check, the member buffer pre-sized for the itemlist, re-run on every activation, while a non-trailing or nested dynamic member stays diagnosed (`struct_dyn_init.pli`, `bad_struct_dyn_init.pli`, ADR-092) |
| (33) | non-data attributes | M8 | prioritize by corpus impact |
| (34)–(38) | `ENTRY`, `RETURNS`, descriptors, `USES`/`SETS` | partial M0 → M2 | ADR-021: scalar C entry/calls served; an external `ENTRY(...) RETURNS(...)` declares a C function whose result types a function reference (`entry_mixed.pli`, ADR-075); a `RETURNS` attribute on a non-ENTRY `DECLARE` item is diagnosed (rule (34), `bad_returns_attr.pli`); fixed-size arrays and structures passed by reference to PL/I procedures (`array_param.pli`, `struct.pli`); dynamic (runtime-extent) arrays passed to procedures via a by-reference bound argument sized at callee entry (`dyn_param.pli`, ADR-054); `*` adjustable-extent parameters whose extent is passed as a hidden argument at each call (`star_param.pli`, ADR-055); a structure-valued function `RETURNS(NAME)` where NAME is an enclosing structure variable whose shape the function takes, returning through a hidden result buffer (`struct_return.pli`, ADR-070); full dope-vector descriptors → M2 |
| (39),(40) | `FILE` attributes | partial M2 | `DECLARE f FILE;` names a file variable that `OPEN`/`CLOSE` and the `FILE ( f )` stream option address (`driver/file`, `bad_file.pli`, ADR-068); the stream file-attributes `INPUT`/`OUTPUT`/`STREAM`/`PRINT` are accepted on `OPEN`, `RECORD`/`UPDATE`/`KEYED`/`ENVIRONMENT` → M6 |
| (41) | `GENERIC` | M8 | generic selection; prioritize by corpus impact |
| (42) | scope (`INTERNAL`/`EXTERNAL`) | M1 | linkage implemented |
| (43) | `LIKE` | M2 | `LIKE <structure-variable>` copies the template's structure shape onto the declared item — top-level `1 T LIKE S;` (T is a copy of S) and nested `2 M LIKE S;` (M is a member of S's shape) both work (`like.pli`, ADR-045); a non-structure or undeclared template is diagnosed, and LIKE combined with a dimension or with members is diagnosed (`bad_like.pli`); qualified LIKE templates `LIKE S.A.B` diagnosed |
| (44)–(55) | `FORMAT` statement and format items | partial M2 (picture items D1) | the common edit-directed format items `F(w,d)`, `A(w)`, `E(w,d)`, `X(w)`, `SKIP(n)`, `PAGE`, and `LINE(n)` are served inside `PUT/GET EDIT` (rule 108, ADR-069/070); `B`/`C`/`P`/`COLUMN`/`R` items, format iteration `(n) (item)`, remote `R(ref)`, and a standalone `FORMAT` statement → M5/D1 |
| (56) | `ENTRY` statement | M1 | `label: ENTRY(params) [RETURNS(...)]` declares an alternate entry point with its own params (any count) and result type; body split into segments behind a shared impl, one thunk per entry name (`entry.pli`); every function-valued entry point shares one result type (the shared impl's type); a void primary may coexist with function ENTRYs, but a plain `RETURN` in a valued impl and `RETURN(value)` in a void impl are diagnosed (`entry_mixed.pli`, `bad_entry_ret.pli`); segments never fall through (each closes on its own return pad); truly mixed return types diagnosed unimplemented (ADR-026, ADR-075) |
| (57)–(59) | statement, unconditional, simple | M0 | |
| (60)–(63) | condition prefixes | parsed M0 → M4 | warned as unenforced |
| (64) | labellist | M0 | label prefixes parsed; used by (7) |
| (65) | initial-label (subscripted labels) | M2/M4 | scan damaged; see ⚠ in grammar |
| (66),(67) | proper-statement, null statement | M0 | |
| (68) | `BEGIN` block | M1 | executes as a real lexical scope; inner declarations shadow outer ones and do not leak (`begin.pli`); block variables are AUTOMATIC in the enclosing procedure's frame (ADR-010, ADR-027) |
| (69)–(73) | `DO` groups, specifications, `WHILE` | M0 | `loops.pli` |
| (74)–(76) | `IF`/`THEN`/`ELSE`, balanced statements | M0 | `ifelse.pli` |
| (77) | `GO TO` | M1/M4 | local `GO TO`/`GOTO` implemented (`goto.pli`, `bad_goto.pli`); non-local to an enclosing procedure → M4 |
| (78)–(80) | `CALL`, options, argumentlist | M0 (opts M9) | `procs.pli`; callee parameters resolve before any body is typed so argument checks apply regardless of procedure order, incl. whole-structure values with dynamic members (`bad_struct_dyn.pli`, ADR-094); `TASK`/`EVENT` → M9 |
| (81) | `RETURN` | M0/M1 | plain `RETURN` (M0); `RETURN(value)` for function procedures (M1, `func.pli`) |
| (82),(83) | `WAIT`, `DELAY` | M9 | |
| (84),(85) | `EXIT`, `STOP` | M0 | |
| (86) | assignment (incl. `BY NAME`) | partial M2 | scalar single target; `SUBSTR` pseudo-variable (`substr_assign.pli`, ADR-024); whole-structure assignment between identical-shape structures (`struct_assign.pli`, rule (127)); multiple assignment `a, b, c = e` with one shared RHS and same-type scalar/array-element targets (`multiassign.pli`, `bad_multiassign.pli`, `bad_multiassign_type.pli`); `BY NAME` assignment `S = T, BY NAME` copies same-named members regardless of layout, recursing into minor structures and arrays, skipping names absent from either side (`struct_by_name.pli`, ADR-043); a same-named dynamic member copies buffer contents with a live-extent mismatch trap while a dynamic-vs-fixed pairing stays diagnosed (`struct_by_name_dyn.pli`, `bad_by_name_dyn.pli`, ADR-093); non-structure or multi-target `BY NAME` diagnosed (`bad_struct_byname.pli`) |
| (87)–(90) | `ALLOCATE`/`FREE` | partial M2 | heap-allocate a based structure and store its address in a POINTER with the `SET(reference)` option, then release the block with `FREE`, addressed either by an explicit locator `P -> X` or by the based variable's own `BASED` pointer — two allocations of the same based variable yield independent blocks (`alloc.pli`, `bad_alloc.pli`, `bad_alloc_set.pli`, ADR-065); the `IN (AREA)` option and dynamic-extent based arrays → QR2.3 |
| (91)–(99) | conditions, `ON`/`REVERT`/`SIGNAL`, `CHECK` | partial M4 | ERROR and SIZE: `ON ERROR [SNAP] (unit \| SYSTEM;)` establishes a handler id (SNAP warned, no effect), `SIGNAL ERROR` runs the top handler then resumes (system action aborts), `REVERT ERROR` pops (empty pop is a no-op), `ONCODE()` reads 1 inside a raised unit else 0 (`on_error.pli`, ADR-076); `ON/SIGNAL/REVERT SIZE` shares the same establish/raise/pop mechanics with a fixed key (`on_size.pli`, ADR-097) and routes every fixed-overflow trap (binary, decimal, float-to-fixed, neg-`INT_MIN`) to the handler when one is established, resuming with the wrapped value (unhandled still aborts; `driver/overflow`, `driver/decimal_overflow`, `driver/float_fixed_overflow`); every other condition diagnosed (`bad_on_cond.pli`); unit frame access, RETURN/nested-ON/DECLARE/ENTRY in units, and GO TO with ON diagnosed (`bad_on_local.pli`); programmer-named `CONDITION(name)` conditions dispatch per condition alongside ERROR (use-declared names, keyed dispatch, no ONCODE coupling; `on_cond.pli`, ADR-080); a name colliding with a declared variable/procedure, and `CONDITION(ERROR)`/`CONDITION(SIZE)`, diagnosed (`bad_on_cond_decl.pli`, `bad_on_cond_error.pli`, `bad_on_cond_size.pli`) |
| (100)–(103) | `OPEN`/`CLOSE` | partial M2 | `OPEN FILE ( f ) TITLE ('name') [INPUT|OUTPUT|STREAM|PRINT]` and `CLOSE FILE ( f )` open/close a named stream (`driver/file`, `bad_file.pli`, ADR-068); `IDENT`/`LINESIZE`/`PAGESIZE`/`ENVIRONMENT` and record forms → M6 |
| (104),(105) | `GET`/`PUT` and options | partial M2 | `PUT [SKIP] [PAGE] LIST`; `GET [SKIP] LIST` list-directed input reading scalar values from SYSIN (`get.pli`, `bad_get.pli`, ADR-066); `STRING ( reference )` routes list-directed output into / input from a NONVARYING character variable (`string.pli`, `bad_string.pli`, ADR-067); complex values print as real, sign, imaginary magnitude, I (`complex_io.pli`, ADR-085) and a `re+imI` token (or bare number, imaginary part zero) reads both parts back (`driver/get_complex`, ADR-086); `FILE ( f )` routes output/input through an opened named file (`driver/file`, `bad_file.pli`, ADR-068); `EDIT` edit-directed transmission is served (see (106)-(111), ADR-069); `DATA`/`COPY`/`LINE` → M5 |
| (106)–(111) | data specifications, data lists | partial M2 | list-directed output (`PUT LIST`) and input (`GET LIST`); edit-directed `EDIT ( datalist ) ( formatlist )` (rule 108) with the numeric `F(w,d)` and `E(w,d)`, the character `A(w)`, and the control `X`/`SKIP`/`PAGE`/`LINE` items for both output and input, each data item paired in order with its `A`/`F`/`E` format and the control items acting between them (`edit.pli`, `e_format.pli`, `bad_edit.pli`, ADR-069/070); `DATA` → M5; format iteration, `B`/`C`/`P`/`COLUMN`/`R`, a standalone `FORMAT` statement, and picture-directed forms → M5/D1 |
| (112),(113) | record I/O | diag → M6 | option set from Y33-6003 (scan incomplete) |
| (114) | `DISPLAY` | partial M5 | `DISPLAY (scalar)` prints one fixed/float/char/bit value plus a newline, ending a pending PUT line first (`display.pli`, ADR-081); non-scalar operands diagnosed (`bad_display.pli`); the `REPLY` form stays diagnosed |
| (115)–(122) | expression precedence hierarchy | M0 | `arith.pli` pins `-3**2` = `-(3**2)` = -9 ((128) constants are unsigned); `usecases/expr.pli` pins negated comparisons and a prefixed `**` exponent |
| (118) | comparison operators | partial M0 | incl. `¬=`, `¬>`, `¬<`; exact part-wise complex `=`/`^=` served while ordered complex comparisons are diagnosed (`complex_arith.pli`, `bad_complex_cmp.pli`, ADR-084) |
| (123) | primitive expressions | partial M2 | constants/vars (M0); function references to function procedures (`func.pli`); array attribute built-ins `LBOUND`/`HBOUND`/`DIM` on fixed-size single-axis arrays (`array_bounds.pli`, `bad_array_builtin.pli`); `DIM` of a multi-axis array = total element count; array reduction built-ins `SUM`/`PROD`/`ANY`/`ALL` over the full extent (`array_reduce.pli`, `bad_array_reduce.pli`); attribute/reduction built-ins also apply to parameter arrays inside a callee (`array_param.pli`); the attribute/reduction built-ins also apply to a qualified structure member array `S.V` (fixed or dynamic-extent), reporting live member bounds and reducing the member buffer (`struct_dyn_len.pli`, ADR-062); pointer built-ins `NULL` (the null pointer) and `ADDR(x)` (the address of a variable) yield POINTER values (`pointer.pli`, ADR-063); scalar math built-ins `FLOOR`/`CEIL`/`SQRT`/`EXP`/`LOG`/`SIN`/`COS`/`TAN`/`LOG2`/`LOG10`/`ATAN`/`SINH`/`COSH`/`TANH`/`ATANH`/`ERF`/`ERFC` — one numeric argument, a FLOAT result, lowered to the matching `<math.h>` runtime wrapper (LOG is the natural logarithm) — and the degree trig variants `SIND`/`COSD`/`TAND`/`ATAND` whose argument/result are in degrees, converted through π in the wrapper (`math.pli`, `math2.pli`, `math3.pli`, `bad_math*.pli`, ADR-072); complex component/conjugate built-ins `COMPLEX(a,b)` (builds a complex value), `REAL(z)`/`IMAG(z)` (extract the real/imaginary part as a FLOAT), and `CONJG(z)` (conjugate) — a complex value is carried as an `{double,double}` pair, now storable: `DECLARE z COMPLEX;` declares a complex variable, real↔complex assignment converts (a real value gets a zero imaginary part; complex→real takes the real part), and `REAL`/`IMAG`/`CONJG` read it back; complex arithmetic and I/O stay unimplemented (`complex.pli`, `complex_var.pli`, `bad_complex.pli`, `bad_complex_var.pli`, ADR-073/074); cross-sections/multi-axis → M2 |
| (124),(125) | locator qualification, qualified names | partial M2 | aggregate (structure) member qualification `S.A.B` in read/write positions and expressions (`struct.pli`); a member of one element of an array of structures is reached by subscript-then-qualify `arr(i).x`, both as a value and as an assignment target (`struct_array_of.pli`, ADR-051); a missing member or wrong subscript count on an array of structures is diagnosed (`bad_struct_array_of.pli`); a minor-structure member is itself a structure value (rule 127) and whole-structure assignment is served (`struct_assign.pli`, `struct_return.pli`); locator-qualified `P -> X` / `P -> X.FIELD` addressing a based variable through a POINTER, on both sides of an assignment (`based.pli`, ADR-064); remaining locators → M3 |
| (126) | subscripted references | partial M2 | fixed-size constant-bounds scalar arrays in read/write positions, single- and multi-axis, compile-time + runtime SUBSCRIPTRANGE on every axis (`array.pli`, `array2d.pli`, `bad_array_oob.pli`, `bad_array2d_oob.pli`, `bad_array2d_arity.pli`); subscripted structure array members `S.A(i)` incl. nested/multi-axis (`struct_array.pli`, `bad_struct_array_*.pli`); a member of one element of an array of structures `arr(i).x` on both sides of an assignment (`struct_array_of.pli`, ADR-051); parameter arrays passed by reference, subscriptable on both sides of an assignment inside a callee (`array_param.pli`); single-`*` cross-sections `A(i, *)`/`A(*, j)` and multi-`*` sub-block copies `A(*, *)`, `D(2, *, *)`, `D(*, 3, *)` (and of an array member `S.A(i, *)`, `S.M(2, *, *)`) assign the reduced lower-rank value to a same-shape whole array via a general affine gather (`cross_section.pli`, `cross_section2.pli`, ADR-046, ADR-049); a cross-section to a scalar or non-matching target, or a cross-section used as a general value is diagnosed (`bad_cross_section.pli`, rule (126)) |
| (127) | unsubscripted reference | partial M2 | whole-structure assignment served as a storage copy between structures of identical shape, incl. array members and nested members (`struct_assign.pli`); a whole structure is an expression value (rule 127) carried by its address: a structure-valued function `RETURNS(NAME)` returns through a hidden result buffer and a `RETURN(struct)` copies into it, a whole-structure argument is passed BY VALUE into a fresh copy, and a structure-returning call is assigned to a same-shape structure (`struct_return.pli`, ADR-070); shape mismatch or mixing a structure with a non-structure diagnosed (`bad_struct_assign.pli`, `bad_struct_value.pli`, rule (127)); full aggregate references → M2 |
| (128),(129) | constants, replicated string constants | partial M0 | replicated strings served; imaginary/sterling → D1 |
| (130)–(133) | identifier, letter, alphameric, digit | M0 | incl. `$ # @` and break character |
| (134) | `isub` (`integer SUB`) | partial M2 | iSUB dummy (`1SUB`, `2SUB`, lexed with no blanks) inside a `DEFINED` subscript base: `DECLARE R(n) DEFINED X(c, 1SUB)` makes R a live 1-D overlay of one axis of X, and `DEFINED X(c, c)` overlays a single element — writes through the overlay are visible in the base and vice versa (`isub_defined.pli`, ADR-048); general iSUB index arithmetic `X(m*1SUB + c)` (affine overlay of one axis) with a constant, multiplier, and offset — the affine image of the overlay range must stay in the base axis (`isub_arith.pli`); more than one iSUB, a scalar item on an iSUB base, a non-affine or out-of-bounds subscript is diagnosed (`bad_isub_arith.pli`, `bad_isub_defined.pli`); multi-axis iSUB → M2 |
| (135)–(139) | integer, fixed/float/imaginary constants | partial M0/M2 | practical binary constants; an exact decimal literal without an exponent (`2.5`) is a `FIXED DECIMAL` constant scaled by 10^q (rule (135), `decimal.pli`); a literal with an exponent stays FLOAT; an imaginary constant `2i` is a complex value with a zero real part (`complex_arith.pli`, ADR-084); decimal/complex I/O completeness → D1/QR2 |
| (140)–(144) | string constants, bit strings, characters | M0 | `strings.pli` (incl. `''` escape) |
| (145) | sterling constants | D1 | with sterling pictures |
| (146)–(148) | picture specification/string/characters | D1 | ADR-017 |
| (149)–(151) | space, comment, comment symbols | M0 | `/* … */` |

## Auxiliary sections

| Section | Feature | Status |
|---|---|---|
| §2.1, §2.2 | notation semantics / meta-syntax | reference only |
| §2.3.1 | generation process, delimiters, 60-char alphabet | M0 (lexer) |
| §2.3.1 | statement-keyword disambiguation | M1 | ADR-004 step 3: `WORD ( … ) =` resolves by a speculative parse of the keyword reading (`ambiguity.pli`); `WORD =` stays an assignment |
| §2.3.2.1 | keyword abbreviations (`DCL`, `PROC`, `BIN`, …) | partial M0 (`DCL`, `PROC`, `BIN`, `DEC`, `CHAR`, `VAR`, `INIT`, `PTR`, `DEF`, `INT`, `EXT`, `CPLX`, `AUTO`) pinned by `abbrev.pli`; abbreviations of unimplemented keywords stay rejected with their keywords → M8 |
| §2.3.2.2 | multiple closure | M0 |
| §2.3.3 | 48-character set: operator words, deletions, colon rules | partial M0 (operator words in `arith.pli`) → M8 |

## C28-6571-3 supplemental coverage

| Section | Feature | Status | Component / test |
|---|---|---|---|
| Chapter 9, The INCLUDE Statement | `%INCLUDE` | QR1 | recursive relative member/path expansion, `.inc` fallback, comments/strings ignored, cycle and missing-member diagnostics (`preprocessor.cpp`, `include.pli`, `bad_include.pli`, `bad_include_cycle.pli`, ADR-071) |
| Chapter 9, remaining processor statements | declarations, replacement, activation, conditionals, loops, transfers, procedures | partial QR2 | `%DECLARE a, b;` integer variables (default 0, include-twice idempotent), `%X = expr;` assignment, and `%IF expr %THEN directive [%ELSE directive]` over literals, variables, arithmetic, comparisons, and `& \|
 ¬` select or drop directive text with recursion for nesting (`pp_if.pli`, `pp_yes.inc`, `pp_no.inc`, ADR-083); undeclared names in `%IF`, missing `%THEN`, and stray `%THEN`/`%ELSE` diagnosed (`bad_pp_if.pli`); `%ACTIVATE`/`%DO`/`%GO TO`/procedures and other stubs still reject (`bad_preprocessor.pli`, ADR-071) |

## IRGen touch-points

Where each implemented feature lands in codegen (`src/irgen.cpp`). The rule →
emit mapping is the same "one feature = N edits" chain as CONTRIBUTING.md; the
entry points below are where that chain terminates.

| Rule(s) | IRGen entry point | Notes |
|---|---|---|
| (1),(2),(3) | `declareProc`, `emitPlainProc`, `emitMultiEntryProc` | procedure shapes and entry aliases |
| (4) | `emitCall` | dummy-argument materialisation for by-ref params |
| (5) | `declareProc` | `MAIN` shim + `RETURNS` function procs |
| (6),(7),(8) | `emitStmt` → `emitAssign`/`emitIf`/… | statement dispatch |
| (9),(10) | `emitGlobals` | `DECLARE` storage |
| (16) | `emitExpr` (`Call`) | `TRUNC` scaled-FIXED guard diagnosed in sema |
| (18) | `loadSym`/`storeTo` | `BIT(1)` held as `i8` in registers (`toI1`) |
| (26) | `emitInitials` | `INITIAL` stores on AUTOMATIC vars; an array itemlist expands via `sym->initElems` to per-element stores (`initValue`), and `emitGlobals` builds a constant-array initializer (`scalarInitConstant`) for STATIC; `INITIAL` on a dynamic array expands the itemlist to `initElems` in sema (no compile-time count check — the extent is runtime) and `emitInitials` GEPs each element into the runtime-sized buffer (`allocaLocals` pass 2 pre-sizes the alloca to the larger of the extent and the itemlist length), re-running on every activation; `INITIAL CALL` (rule 27) evaluates the lowered call (`sym->initCallH`) via `emitExpr` at block entry and stores the return value |
| (34) | `calleeFn` | external C `ENTRY` decl (rule 38 interop) |
| (56) | `emitMultiEntryProc`, `entryIrName` | alternate entry thunks |
| (68) | `emitStmt` (`Begin`) | block body executed as a group |
| (69)–(73) | `emitDoWhile`, `emitDoIter` | `DO WHILE` / iterative |
| (74)–(76) | `emitIf` | |
| (77) | `collectGotoBlocks`, `emitStmt` (`Goto`) | local `GO TO` |
| (78),(79) | `emitCall` | `CALL` statement |
| (81) | `emitStmt` (`Return`) | value / plain `RETURN` |
| (84),(85) | `emitStmt` (`Stop`) | `pli_stop` + `Unreachable` |
| (86) | `emitAssign`, `storeTo` | incl. `SUBSTR` pseudo-variable (`pli_substr_assign`); multiple assignment evaluates the RHS once and stores to each target (`emitAssign` multi-target path); `BY NAME` emits member-by-name copies through `emitByNameCopy` (scalar load/convert/store, memcpy for arrays, recursion into minor structures) |
| (104),(105) | `emitPut`, `emitGet` | list-directed output/input; `STRING ( reference )` routes through `pli_string_put_open/close` (PUT) or `pli_string_get_open/close` (GET), `FILE ( f )` through `pli_put_select/unselect` (PUT) or `pli_get_select/unselect` (GET), reusing the same per-type `pli_put_list_*`/`pli_get_list_*` functions via a runtime sink/source switch (`put_raw`/`next_char`) |
| (115)–(122) | `emitExpr` (binary/`Unary`) | arithmetic, bit, comparison, concat; complex `+ - * /` over pairs with mixed-real conversion and exact `=`/`^=` (`complex_arith.pli`, ADR-084) |
| (123) | `emitExpr` (`Call`) | per-builtin handlers: SUBSTR, INDEX, ABS, LENGTH, TRUNC, PRECISION, MIN, MAX, MOD, MULTIPLY, DIVIDE, ROUND, REPEAT, VERIFY, TRANSLATE, HIGH, LOW, DATE, TIME, LBOUND, HBOUND, DIM, SUM, PROD, ANY, ALL, NULL, ADDR, FLOOR, CEIL, SQRT, EXP, LOG, SIN, COS, TAN, LOG2, LOG10, ATAN, SINH, COSH, TANH, ATANH, ERF, ERFC, SIND, COSD, TAND, ATAND, ASIN, ACOS, ATAN2, CBRT, COMPLEX, REAL, IMAG, CONJG |
| (12),(13),(126) | `arrayExtent`, `arrayElementAddr`, `loadArrayElement`, `storeArrayElement` | `[N x elemTy]` flat row-major layout; multi-axis GEP offset = Σ (i_k − lb_k)·stride_k; per-axis SUBSCRIPTRANGE; `DIM` = product of extents; a structure array member lays out as `[N x elemTy]` inside the struct (`llvmTy` handles `isArray`) and is subscripted via `memberAddr` + `arrayElementAddr`; a single-axis dynamic array `A(n)` allocates a runtime-sized element buffer (`alloca i32, i64 extent`) from its bound expression evaluated at entry, records the live bounds (`dynUb_`/`dynLb_`, params via `recordDynParamUbs`) for runtime SUBSCRIPTRANGE and runtime `LBOUND`/`HBOUND`/`DIM` (`allocaLocals` pass 2, `arrayElementAddr` dynamic path); with `INITIAL` the runtime-sized alloca is pre-sized to the larger of the extent and the itemlist length (the logical extent used for bounds checks is unchanged), and pass 2's buffer is filled element-by-element at entry (`emitInitials`); a dynamic array structure member lays out as a bare buffer pointer field (`llvmTy` emits `ptr` for an `isArray && isDynamic` member), allocated at entry by pass 3 of `allocaLocals` (bounds recorded in `memberDyn_`, pointer stored into the field), and subscripted by loading the buffer pointer (`dynamicMemberBase`) before the `arrayElementAddr` dynamic path; an array of structures `arr(i).x` lays out as `[N x structTy]` and a member of one element is addressed by `arrayElementAddr` to the element struct then `elementMemberAddr` (a GEP through the member path from the element address) |

## Extensions (not in TR 25.084)

| Extension | Status | Component / test |
|---|---|---|
| `%REPLACE` (ADR-077) | served | `%REPLACE name BY <tokens> ;` substitutes an identifier with source text before parsing (`replace.pli`); a lone `"..."` operand is re-scanned as source text (`replace_quoted.pli`, ADR-082); stray double quotes and mixed quoted/raw replacements diagnosed (`bad_dqstring.pli`, `bad_replace_mixed.pli`); other `%` directives and malformed directives diagnosed (`bad_replace.pli`) |
| `%INCLUDE` search paths (ADR-078) | served | including file's directory, then repeatable `-I` (first wins, `-I<dir>` too), then `PLIC_INCLUDE_PATH`, then the executable-relative `share/plic/include` default; `-v` lists the configured dirs (`driver/include_dirs`) |

## Headline numbers (M0)

- Rules fully implemented and tested: **40**
- Rules partially implemented: **18**
- Rules recognised and diagnosed with their number: **21**
- Rules not yet reached: **72**
